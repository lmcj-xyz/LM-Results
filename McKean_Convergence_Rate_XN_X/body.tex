\section{What is this?}

An adaptation of
\cite[Proposition 3.1]{de_angelis_numerical_2020}
for the case of
\cancel{McKean SDEs}
\ei{not yet for McKean SDEs, here you are only looking at SDEs with distributional coefficients in Besov spaces, SDEs framed as martingale problems}
SDEs with drift in a Besov space of negative order
proposed in
\cite{issoglio_pde_nodate}
and
\cite{issoglio_mckean_2021}.

The proof builds on a number of results presented in the sections below.

\section{Some useful definitions and results}

Here we present some results and definitions to refer on the text.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Definition Local time
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{definition}{Local time at zero}\label{def:local_time_zero}
  For any real-valued continuous semi-martingale
	$ \bar{Y} $,
	the local time at zero
  $L^{0}_{t} (\bar{Y})$
  is defined as
  \begin{equation}
    L^0_t (\bar{Y}) = \lim_{\epsilon \to 0} \frac{1}{2 \epsilon} \int_0^t \mathbbm{1}_{\{|\bar{Y}| \leq \epsilon\}} d \langle \bar{Y} \rangle_s
    \text{, }
    \mathbb{P}
    \text{-a.s.}
  \end{equation}
  For all
  $t \geq 0$.
\end{definition}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Local time at zero, Lemma 5.1
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
The first result,
\cite[Lemma 5.1]{de_angelis_numerical_2020},
is not necessary to prove for this particular setting since the result holds for any semi-martingale, I include it here for self-containment reasons.
\ei{
				Instead of this sentence you should write something like 'The lemma below is from
				\cite{de_angelis_numerical_2020}
				and its proof can be found in
				\cite[Lemma 5.1]{de_angelis_numerical_2020}.
				We include the statement here for ease of reading'
}

\begin{lemma}{Bound for local time at zero for a semi-martingale}\label{lemma:local-time-at-0}
%%%%\todo{Type down this lemma}
For any
$\epsilon \in (0,1)$
and any real-valued, continuous semi-martingale
$Z$
we have

\begin{align*}
    \mathbb{E}[L^0_t(Z_s)] \leq & 4 \epsilon
		- {\color{red}2} \mathbb{E} \left[ \int_0^t \left( \mathbbm{1}_{\{Z_s \in (0, \epsilon)\}} + \mathbbm{1}_{\{Z_s \geq \epsilon\}} e^{1 - Z_s/\epsilon} \right) dZ_s \right]
    \\
    &+ \frac{1}{\epsilon} \mathbb{E} \left[ \int_0^t \mathbbm{1}_{\{Z > \epsilon} e^{1 - Z_s/\epsilon\}} d \langle Z \rangle_s \right].
\end{align*}

\end{lemma}

\ei{
				It is confusing to call the semimartingale
				$ \bar{Y} $
				above and
				$ Z $
				in the lemma.
				It is not wrong, but not the best use of notation.
}

Let us introduce the original and regularised Kolmogorov equations.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Kolmogorov equations
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{definition}{Kolmogorov equations}\label{def:kolmogorov_eqns}

For
$\beta \in (0, 1/2)$
let
$b \in C_T \mathcal{C}^{-\beta}$,
$u, u^N \in C_T\mathcal{C}^{(1 + \beta)+}$, 
and
$b^N \to b$
as
$N \to \infty$
in
$C_T \mathcal{C}^{-\beta}$
	The equations
	\ei{
					EI: mistake in PDEs, correct please!!
	}

	\begin{equation}
	\begin{cases}
	\partial u_i + \frac{1}{2} b_i \Delta u_i = \lambda u_i - b_i
	\\
	u_i(T) = 0,
	\end{cases}
	\end{equation}
	
	\begin{equation}
	\begin{cases}
	\partial u^N_i + \frac{1}{2} b^N_i \Delta u^N_i = \lambda u^N_i - b^N_i
	\\
	u^N_i(T) = 0.
	\end{cases}
	\end{equation}

	are called Kolmogorov and regularised Kolmogorov equations.
	Here written component wise.
\end{definition}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Bounds for the gradient of u
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{lemma}{Bounds for $ \nabla u, \nabla u^N $}\label{lemma:bounds_gradients}
	Let
	\todo{type this result which is lemma 4.2 in the paper}
\end{lemma}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Lemma 5.2
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Bounds for the difference of solutions to the Kolmogorov equations}

We need a bound for
$u - u^N$
and
$\nabla u - \nabla u^N$
in
$L_\infty$ 
for the case in which 
$u \in C_T \mathcal{C}^{1+\alpha}$
for some
$\alpha > \beta$
which is an adaptation of
\cite[Lemma 5.2]{de_angelis_numerical_2020}.

The result builds on top of the following result:

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Bound for the rho-equivalent norm of the difference u-uN
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{proposition}
{Bound for the $\rho$-equivalent norm of $u - u^N$}
\label{prop:diff_u_uN}

Let
$u, u^N$
be (mild) solutions to the Kolmogorov equations from
\ref{def:kolmogorov_eqns}
then as
$N \to \infty$

\begin{equation}
\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{1 + \alpha}}
	\leq
	\frac{c T^{\frac{1 - \beta - \alpha}{2}} \| b_i - b^N_i \|_{C_T\mathcal{C}^{-\beta}} (\| u_i \|_{C_T\mathcal{C}^{1 + \alpha}} - 1) }{1 - c \rho^{\frac{\alpha + \beta - 1}{2}} (\| b \|_{C_T\mathcal{C}^{-\beta}} + \lambda)}
\end{equation}

for
$\rho \geq \rho_0$,
where

\begin{equation}
	\rho_0 = 2 c ( \|  b_i \|_{C_T \mathcal{1+\alpha}} + \lambda ) ^ { \frac{ 2 }{ \alpha + \beta + 1}}
\end{equation}

and
$\lambda > 0$.

\end{proposition}

\begin{proof}

	See that
	$u^N(T) = u(T) = 0$,
	and in
	\cite{issoglio_pde_nodate},
	set
	$g^N, g$
	as
	$b^N, b$
	respectively.
	See that
	$b^N \to b$.
	Then let us reformulate the rest of the aforementioned result for
	$\lambda \neq 0$.

	As
	$u^N, u$
	are mild solutions, we have

	\begin{align*}
		u_i(t) - u^N_i(t)
		&=
		P_{T-t} (u_i(T) - u^N_i(T))
		\\
		&+
		\int_t^T P_{s-t}(\nabla u_i b_i - \nabla u^N_i b^N_i) ds
		\\
		&-
		\int_t^T P_{s-t}(\lambda u_i + b_i - \lambda u^N_i + b^N_i) ds
		\\
		&=
		\int_t^T P_{s-t}(\nabla u_i b_i - \nabla u^N_i b^N_i) ds
		\\
		&-
		\lambda  \int_t^T P_{s-t}(u_i - u^N_i) ds
		\\
		&-
		\int_t^T P_{s-t}(b_i - b^N_i) ds
		\\
		&=
		\int_t^T P_{s-t}[(\nabla u_i b_i - \nabla u_i b^N_i) + (\nabla u_i b^N_i - \nabla u^N_i b^N_i)] ds
		\\
		&-
		\lambda  \int_t^T P_{s-t}(u_i - u^N_i) ds
		\\
		&-
		\int_t^T P_{s-t}(b_i - b^N_i) ds
		\\
		&=
		\int_t^T P_{s-t}(\nabla u_i b_i - \nabla u_i b^N_i)  ds
		\\
		&+
		\int_t^T P_{s-t}(\nabla u_i b^N_i - \nabla u^N_i b^N_i) ds
		\\
		&-
		\lambda  \int_t^T P_{s-t}(u_i - u^N_i) ds
		\\
		&-
		\int_t^T P_{s-t}(b_i - b^N_i) ds
	\end{align*}

	Now let us compute the $\rho$-equivalent norm of
	$u-u^N$,
	for some
	$\alpha > \beta$
	
	
    \todo{this norm is wrong, should be $1+\alpha$ on the lhs and everywhere else}
	\begin{align*}
		\| u_i - u^N_i \| ^ {(\rho)}_{C_T \mathcal{C}^{-\beta}}
		&=
		\sup_{0\leq t \leq T} e^{-\rho(T-t)} \| u(t) - u^N(t) \|_{1 + \alpha}
		\\
		&\leq
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} \| \int_t^T P_{s-t}(\nabla u_i b_i - \nabla u_i b^N_i)  ds \|_{1+\alpha}
	     	\\ 
		&+
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} \| \int_t^T P_{s-t}(\nabla u_i b^N_i - \nabla u^N_i b^N_i) ds\|_{1+\alpha}
	     	\\ 
		&-
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} \| \lambda  \int_t^T P_{s-t}(u_i - u^N_i) ds\|_{1+\alpha}
	     	\\ 
		&-
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} \| \int_t^T P_{s-t}(b_i - b^N_i) ds\|_{1+\alpha}
		.
	\end{align*}

	Let us take each term from the right hand side of the inequality and bound them.

	For the first term, using
	$\gamma + 2\theta = 1 + \alpha$,
	$\gamma = -\beta$,
	$\theta = \frac{1 + \alpha + \beta}{2}$,
	$\| P_t f \|_{\gamma + 2 \theta} \leq c t ^{-\theta} \| f \|_{\gamma} \|$
	and
	$\| \nabla g \|_\xi \leq c \|g \|_{\xi + 1}$

	\begin{align*}
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} 
		&\norm{ \int_t^T P_{s-t}(\nabla u_i b_i - \nabla u_i b^N_i)  ds }_{1+\alpha}
		\\
		&\leq
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} \int_t^T (s - t)^{-\theta} \| \nabla u_i\|_\alpha \| b_i - b^N_i \|_{-\beta}
		\\
		&\leq
		c \| u_i \|_{C_T \mathcal{C}_{1 + \alpha}} \| b_i - b^N_i \|_{C_T \mathcal{C}^{-\beta}} \sup_{0 \leq t \leq T} e^{-\rho(T-t)}(T-t)^{\frac{1 - \beta -\alpha}{2}}
		\\
		&\leq
		c T^{\frac{1 - \beta - \alpha}{2}} \| u_i \|_{C_T \mathcal{C}_{1 + \alpha}} \| b_i - b^N_i \|_{C_T \mathcal{C}^{-\beta}}
	\end{align*}

	For the second term, see that for
	$N \to \infty$,
	we have
	$\| b^N \|_{C_T \mathcal{C}^{-\beta}} \leq 2 \| b \|_{C_T \mathcal{C}^{-\beta}}$

	\begin{align*}
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} 
		&\norm{ \int_t^T P_{s-t} b^N_i(\nabla u_i - \nabla u^N_i ) ds}_{1+\alpha}
		\\
		&\leq
		c \sup_{0\leq t\leq T} \int_t^T(s - t)^{-\theta} e^{-\rho(T-t)} 2 \| b_i \|_{-\beta} \| \nabla u_i - \nabla u^N_i \|_{\alpha} ds
		\\
		&\leq
		c \| b_i \|_{C_T \mathcal{C}^{-\beta}} \| u_i - u^N_i \|_{C_T \mathcal{C}^{-\beta}}^{(\rho)} \int_t^T(s - t)^{-\theta} e^{-\rho(T-t)} ds
		\\
		&\leq
		c \| b_i \|_{C_T \mathcal{C}^{-\beta}} \| u_i - u^N_i \|_{C_T \mathcal{C}^{-\beta}}^{(\rho)} \rho^{\frac{\alpha + \beta - 1}{2}}
	\end{align*}

	For the third term, which is the one that difers from the proof in
	\cite{issoglio_pde_nodate}
	we need to use that
	$\| P_t f \|_{\gamma + 2\theta} \leq ct^{-\theta} \| f \|_{\gamma}$,
	and in this case we have
	$\gamma + 2 \theta = 1 + \alpha$
	and
	$\gamma = 1 + \alpha$,
	so that
	$\theta = 0$
	because
	$u, u^N \in C_T \mathcal{C}^{1+\alpha}$,
	so we will have

	\begin{align*}
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} 
		&\norm{ \lambda  \int_t^T P_{s-t}(u_i - u^N_i) ds}_{1+\alpha}
		\\
		&\leq
		c \lambda \sup_{0\leq t\leq T} e ^{-\rho(T-t)} \int_t^T (s - t)^{-0} \| u_i - u^N_i \|_{1 + \alpha} ds
		\\
		&=
		c \lambda \sup_{0\leq t\leq T} e ^{-\rho(T-t)} \int_t^T e ^{-\rho(T-s)} \sup_{0\leq s\leq T} e ^{-\rho(T-s)} \| u_i - u^N_i \|_{1 + \alpha} ds
		\\
		&=
	c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \int_t^T e ^{-\rho(T-s)} e ^{-\rho(T-t)} ds
		\\
		&=
	c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \int_t^T e^{-\rho(s-t)} ds
		\\
		&=
		c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \sup_{0\leq t\leq T} \rho^{-1} [1 - e^{-\rho(T-t)}]
		\\
		&\leq
		c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \rho^{-1}
		\\
		&\leq
		c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \rho^{\frac{\alpha+\beta-1}{2}}
	\end{align*}

	And for the last term
	\begin{align*}
		\sup_{0\leq t\leq T} e ^{-\rho(T-t)} &\norm{ \int_t^T P_{T-s}(b_i - b^N_i) ds}_{1+\alpha}
		\\
		&\leq
		c \sup_{0\leq t\leq T} e ^{-\rho(T-t)} \int_t^T (s - t)^{-\frac{\alpha+\beta-1}{2}} \| b_i - b^N_i \|_{-\beta} ds
		\\
		&\leq
		c \| b_i - b^N_i \|_{C_T \mathcal{C}^{-\beta}} \sup_{0\leq t\leq T} e ^{-\rho(T-t)} (s - t)^{-\frac{\alpha+\beta-1}{2}}
		\\
		&\leq
		c T^{\frac{1-\beta-\alpha}{2}} \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}}
	\end{align*}

	Putting everything together

	\begin{align*}
		\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{-\beta}} 
		&\leq
		c T^{\frac{1-\beta-\alpha}{2}} \|u_i\|_{C_T \mathcal{C}^{1+\alpha}} \| b_i - b^N_i \|_{C_T \mathcal{C}^{-\beta}}
			\\
		&+
		c \| b_i \|_{C_T \mathcal{C}^{-\beta}} \| u_i - u^N_i \|_{C_T \mathcal{C}^{-\beta}}^{(\rho)} \rho^{\frac{\alpha + \beta - 1}{2}}
		\\
		&-
		c \lambda \sup_{0\leq t\leq T} \| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}_{1 + \alpha}} \rho^{\frac{\alpha+\beta-1}{2}}
		\\
		&-
		c T^{\frac{1-\beta-\alpha}{2}} \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}},
		\\
	\end{align*}

and finally,

	\begin{align*}
		\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{-\beta}} (1 - c \rho^{\frac{\alpha + \beta - 1}{2}} [ \| b \|_{C_T \mathcal{C}^{-\beta}} &+ \lambda])
		\\
		&\leq
		cT^{\frac{1-\beta-\alpha}{2}} \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}} (\| u_i \|_{C_T \mathcal{C}^{1+\alpha}} -1)
		\\
		\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{-\beta}} 
		&\leq
	\frac{cT^{\frac{1-\beta-\alpha}{2}} \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}} (\| u_i \|_{C_T \mathcal{C}^{1+\alpha}} -1)}{(1 - c \rho^{\frac{\alpha + \beta - 1}{2}} [ \| b \|_{C_T \mathcal{C}^{-\beta}} + \lambda])}
	\end{align*}

	As required.
\end{proof}

Note that in the above we can represent the right hand side of the inequality as

\begin{equation}
	\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{-\beta}} 
	\leq
	\frac{cT^{\frac{1-\beta-\alpha}{2}} (\| u_i \|_{C_T \mathcal{C}^{1+\alpha}} -1)}{(1 - c \rho^{\frac{\alpha + \beta - 1}{2}} [ \| b \|_{C_T \mathcal{C}^{-\beta}} + \lambda])} \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}} 
\end{equation}

	\todo{Check this norm}
\begin{equation}
	\| u_i - u^N_i \|^{(\rho)}_{C_T \mathcal{C}^{-\beta}} 
	\leq
	c(\rho) \| b_i - b^N_i \|_{C^T \mathcal{C}^{-\beta}} 
\end{equation}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Adaptation of Lemma 5.2
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
Here is the adaptation of
\cite[Lemma 5.2]{de_angelis_numerical_2020}.

\begin{proposition}
{Bounds for $\|u - u^N\|_{L_\infty}$ and $\|\nabla u - \nabla u^N\|_{L_\infty}$}
\label{prop:diff_uN_graduN}

Let
$\beta \in (0, 1/2)$
and
$b \in C_T \mathcal{C}^{-\beta}$.
Let 
$u, u^N \in C_T\mathcal{C}^{(1 + \beta)+}$
be (mild) solutions to the Kolmogorov equations from
\ref{def:kolmogorov_eqns}.

Assume, by
\ref{prop:diff_u_uN},
that for some 
$\alpha > \beta$

\begin{equation}
	\| u - u^N \|^{(\rho)}_{C_{T} \mathcal{C}^{1+\alpha}}
	\leq
	c(\rho)
	\| b - b^N \|_{C_{T} \mathcal{C}^{-\beta}}
	.
	\label{eq:u-uNb-bN}
\end{equation}

With
$c(\rho)$
as in
\ref{prop:diff_u_uN}
and
$\rho_0$
is large enough such that
$c(\rho) > 0$
for all
$\rho > \rho_0$.

Then for all
$t \in [0, T]$

\begin{equation}
	\| u^N(t) - u(t) \|_{L^\infty}
	\leq
	\kappa_\rho \| b - b^N \|_{C_T\mathcal{C}^{-\beta}}
	\label{eq:uNu_bounded_by_bNb}
\end{equation}

\begin{equation}
	\| \nabla u^N(t) - \nabla u(t) \|_{L^\infty}
	\leq
	\kappa_\rho \| b - b^N \|_{C_T\mathcal{C}^{-\beta}}
	\label{eq:graduNu_bounded_by_bNb}
\end{equation}
with
$\kappa_\rho = c \cdot c(\rho) \cdot e^{\rho T}$.

\end{proposition}

\begin{proof}
    First let us prove
    \ref{eq:uNu_bounded_by_bNb}.
    
    Let
    $t \in [0, T]$,
    and see that since
    $u, u^N \in C_T \mathcal{C}^{(1 + \beta)+}$
    there exists
    $\alpha > \beta$
    such that
    $u, u^N \in C_T \mathcal{C}^{1 + \alpha}$,
    then for any
    $f \in \mathcal{C}^{1 + \alpha}$
    we have
    \begin{equation}
        \| f \|_{C^{1 + \alpha}}
        \leq
        c
        \left(
        \sup_{x \in \mathbb{R}^d} | f(x) |
        + \sup_{x \neq y \in \mathbb{R}^d}
        \frac{|\nabla f (x) - \nabla f(y)|}{|x - y|^\alpha}
        \right)
    \end{equation}
    
    so we have
    
    \begin{equation}
    \begin{split}
        \| u^N(t) - u(t) \|_{L^\infty}
        &=
        \sup_{x \in \mathbb{R}^d} | u^N(t, x) - u(t, x) |
        \\
        &\leq
        c \| u^N(t) - u(t) \|_{\mathcal{C}^{\alpha + 1}}
    \end{split}
    \label{eq:u-uN_in_Linfinity_bounded_by_u-uN_in_Calpha1}
    \end{equation}
    
    Moreover, using the
    $(\rho)$-equivalent
    norm
    \begin{equation}
        \| f \|_{\mathcal{C}^{1 + \alpha}} = \sup_{t \in [0, T]} e^{- \rho (T - t)} \norm{f(t)}_{\mathcal{C}^{1 + \alpha}},
    \end{equation}
    and
    \ref{eq:u-uNb-bN}
    we see that
    \begin{equation}
        \begin{split}
            \norm{u^N - u}_{C_T \mathcal{C}^{1 + \alpha}}
            &=
            \sup_{t \in [0,T]}\norm{u^N - u}_{\mathcal{C}^{1 + \alpha}}
            \\
            &=
            \sup_{t \in [0,T]} e^{\rho(T- t)} e^{-\rho(T- t)} \norm{u^N - u}_{\mathcal{C}^{1 + \alpha}}
            \\
            &\leq
            e^{\rho T} \sup_{t \in [0,T]} e^{-\rho(T- t)} \norm{u^N - u}_{\mathcal{C}^{1 + \alpha}}
            \\
            &=
            e^{\rho T} \norm{u^N - u}_{C_T \mathcal{C}^{1 + \alpha}}^{(\rho)}.
        \end{split}
        \label{eq:norm_bounded_by_rho_norm}
    \end{equation}
    
    Plugging
    \ref{eq:norm_bounded_by_rho_norm}
    into
    \ref{eq:u-uN_in_Linfinity_bounded_by_u-uN_in_Calpha1}
    
    \begin{equation}
    \begin{split}
        \| u^N(t) - u(t) \|_{L^\infty}
        &\leq
        c \| u^N(t) - u(t) \|_{\mathcal{C}^{\alpha + 1}}
        \\
        &\leq
        \sup_{t \in [0,T]} c \norm{u^N(t) - u(t)}_{\mathcal{C}^{\alpha + 1}}
        \\
        &=
        c \norm{u^N - u}_{C_T \mathcal{C}^{\alpha + 1}}
        \\
        &\leq
        c e^{\rho T} \norm{u^N - u}_{C_T \mathcal{C}^{\alpha + 1}}^{(\rho)}.
    \end{split}
    \end{equation}
    
    And finally by
    \ref{eq:u-uNb-bN}
    
    \begin{equation}
        \norm{u^N(t) - u(t)}_{L^\infty}
        \leq
        c \cdot c(\rho) \cdot e^{\rho T} \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}}
    \end{equation}
    
    which proves
   \ref{eq:uNu_bounded_by_bNb}.
   
   For
   \ref{eq:graduNu_bounded_by_bNb}
   recall that if
   $f \in \mathcal{C}^{1 + \alpha}$
   then
   $\nabla f \in \mathcal{C}^{\alpha}$.
   Also, by Bernstein inequality
   \cite[Eqn. (9)]{issoglio_mckean_2021}
   \begin{equation}
    \norm{\nabla f}_{\mathcal{\alpha}}
    \leq
    c \norm{f}_{\mathcal{1+ \alpha}}.
   \end{equation}
   
  Using the equivalent norm
    \begin{equation}
        \| f \|_{C^{1 + \alpha}}
        \leq
        c
        \left(
        \sup_{x \in \mathbb{R}^d} | f(x) |
        + \sup_{x \in \mathbb{R}^d} | \nabla f(x) |
        + \sup_{x \neq y \in \mathbb{R}^d}
        \frac{|\nabla f (x) - \nabla f(y)|}{|x - y|^\alpha}
        \right)
    \end{equation}
    
    we can see that
    
    \begin{equation}
        \begin{split}
        \norm{\nabla u^N(t) - \nabla u(t)}_{L^\infty}
        &\leq
        c \norm{u^N(t) - u(t)}_{\mathcal{C}^{1+\alpha}}.
        \end{split}
    \end{equation}
    
    And usign the same bounds that we used above for
    $c \norm{u^N(t) - u(t)}_{\mathcal{C}^{1+\alpha}}$
    this point follows.
    
\end{proof}

\section{Bound for the difference of the auxiliary functions}
This is the adaptation of result
\cite[Lemma 5.3]{de_angelis_numerical_2020}.

\begin{proposition}{Bound for $ \abs{\psi (t,x) - \psi^N(t,x) }$}\label{prop:bound_aux_psi}

    \label{prop:bound_psi-psiN}
    Take
    $\rho > \rho_0$
    as in
    \ref{prop:diff_u_uN},
    $N \to \infty$,
    $\kappa_\rho$
    from
    \ref{prop:diff_uN_graduN},
    and
    $\beta \in (0, 1/2)$,
    then we have
\begin{equation}
    \sup_{(t,x) \in [0,T] \times \mathbb{R}}
    \abs{\psi (t,x) - \psi^N(t,x)} \leq 2 \kappa_\rho \norm{b - b^N}_{C_T\mathcal{C}^{-\beta}}
\end{equation}
\end{proposition}

\begin{proof}
    Recall the definition of
    $\psi, \phi \in C_T \mathcal{C}^{1} $
    
    \begin{equation}
        \phi(t, x) := x + u(t, x)
    \end{equation}
    
    \begin{equation}
        \psi(t, \cdot) = \phi^{-1} (t, \cdot).
    \end{equation}
    
    Note that
    
    \begin{equation}
        u(y) = \int_0^1 \nabla u(\alpha y) y d \alpha + u(0).
    \end{equation}
    
    From there we have

    \begin{equation}
	    u(t, y) - u(t, y') = \int_0^1 \nabla u (t, \alpha(y - y')) (y - y') d \alpha
    \end{equation}

    and therefore

    \begin{equation}
	    \abs{u(t, y) - u(t, y')} 
	    \geq
	    \left(\int_0^1 \abs{ \nabla u (t, \alpha(y - y')) }^2 d \alpha \right)^{1/2} \abs{y - y'},
    \end{equation}

    and by
    \ref{lemma:bounds_gradients}
    we finally have

    \begin{equation}
	    \begin{split}
		    \abs{u(t, y) - u(t, y')}
		    &\leq
		    \left(\frac{1}{4} \int_0^1 d \alpha \right)^{1/2} \abs{y - y'}
		    \\
		    \abs{u(t, y) - u(t, y')}^2
		    &\leq
		    \frac{1}{4} \abs{y - y'}^2
	    \end{split}
    \end{equation}
    \todo{continue from page three in notes}

\end{proof}

\section{Bound for the local time at zero of the solution to the SDEs}

We need a bound for
$\mathbb{E} [L^0_T ( Y^N - Y ) ]$,
for Sobolev spaces, this is result
\cite[Proposition 5.4]{de_angelis_numerical_2020}
we present it here for the solutions to the SDE blonging to the appropriate Besov spaces.

\begin{proposition}
				{Bound for local time of solutions to the SDE}
				\label{prop:bound_local_time_sde}
	Let
	$b \in C_T \mathcal{C}^{-\beta}$
	and
	$b^N \to b$
	in
	$C_T \mathcal{C}^{-\beta}$
	as
	$N \to \infty$
	for
	$\beta \in (0, \frac{1}{4})$
	and for any
	$\alpha > \beta$


\end{proposition}

\begin{proof}
				\ei{
								Make sure that you write
								$ \lambda $
								and not
							  $ 1 + \lambda $	
				}
	Recall that
	$Y^N, Y$
	are solutions to the SDEs
	\begin{equation}
		Y_t = y_0 + \lambda \int_0^t u(s, \psi(s, Y_s)) ds + \int_0^t ( \nabla u (s, \psi(s, Y_t)) + 1 ) dW_s
	\end{equation}
	and
	\begin{equation}
		Y^N_t = y^N_0 + \lambda \int_0^t u^N(s, \psi^N(s, Y^N_s)) ds + \int_0^t ( \nabla u^N (s, \psi^N(s, Y^N_t)) + 1 ) dW_s
	\end{equation}
	so that the difference
	$Y^N - Y$
	is
	\begin{equation}
		\begin{split}
			Y^N - Y_t &= 
		(y^N_0 + \lambda \int_0^t u^N(s, \psi^N(s, Y^N_s)) ds + \int_0^t ( \nabla u^N (s, \psi^N(s, Y^N_t)) + 1 ) dW_s)
		\\
				  &-
		(y_0 + \lambda \int_0^t u(s, \psi(s, Y_s)) ds + \int_0^t ( \nabla u (s, \psi(s, Y_t)) + 1 ) dW_s)
		\\
				  &=
		(y^N_0 - y_0) + \lambda \int_0^t ( u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y_s)) ) ds
		\\
				  &-
		\int_0^t ( \nabla u^N (s, \psi^N(s, Y^N_t)) - \nabla u (s, \psi(s, Y_t))) dW_s,
		\end{split}
	\end{equation}

	and using
	\ref{lemma:local-time-at-0}
	we have the following bound
	
	\begin{equation}
		\begin{split}
			&\mathbb{E} [L_t^0 (Y^N - Y)]
			\\
			&\leq
			4 \epsilon +1
			\\
			&-
			2(1+\lambda)
			\mathbb{E}
			\qty[
				\int_0^t
				\qty( 
				\mathbbm{1}_{\qty{Y^N_s - Y_s \in (0, \epsilon)}} 
				+ \mathbbm{1}_\qty{Y^N_s - Y_s \geq \epsilon} e^{1 - \frac{Y^N_s - Y_s}{\epsilon}}
					)
			\qty( u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y_s))) ds
			]
		      	\\
			&+
			\frac{1}{\epsilon}
			\mathbb{E}
			\qty[
			\int_0^t \mathbbm{1}_\qty{Y^N_s - Y_s > \epsilon} e^{1 - \frac{Y^N_s - Y_s}{\epsilon}}
			\qty( \nabla u^N(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s)))^2 ds
			].
		\end{split}
		\label{eq:local_time_diff}
	\end{equation}

	\todo{add the explanation of why to drop the difussion term}

	For the second and third terms of
	\ref{eq:local_time_diff}
	let us bound the factors involving the differences of
	$u, u^N$
	and
	$\nabla u, \nabla u^N$.

	First, for $u, u^N$ adding and subtracting terms and using triangle inequality we have

	\begin{equation}
		\begin{split}
			\abs{u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y_s))}
			&\leq
			\abs{u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi^N(s, Y^N_s))}
			\\
			&+
			\abs{u(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y^N_s))}
			\\
			&+
			\abs{u(s, \psi(s, Y^N_s)) - u(s, \psi(s, Y_s))}.
		\end{split}
	\end{equation}
\end{proof}

The terms in the right hand side will be bounded as follows:

\begin{itemize}
\item
For the first term, by
\ref{prop:diff_uN_graduN}

\begin{equation}
  \abs{u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi^N(s, Y^N_s))}
	\leq
	\norm{ u^N(s) - u(s) }_{L^\infty}
	\leq
	\kappa_\rho \norm{ b - b^N }_{C_T\mathcal{C}^{-\beta}},
\end{equation}

\item
for the second term, observe that
$u, u^N$
are
$\frac{1}{2}$-Lipschitz
and by
\ref{prop:bound_psi-psiN}
we get

\begin{equation}
  \begin{split}
    \abs{u(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y^N_s))}
    &\leq
    \frac{1}{2}\abs{\psi^N(s, Y^N_s) - \psi(s, Y^N_s)}
    \\
    &\leq
    \kappa_\rho \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}},
  \end{split} 
\end{equation}

\item
and for the final term, note that
$\psi, \psi^N$
are
2-Lipschitz

so that

\begin{equation}
  \abs{u(s, \psi(s, Y^N_s)) - u(s, \psi(s, Y_s))}
	\leq
	\frac{1}{2} \abs{\psi(s, Y^N_s) - \psi(s, Y_s)}
	\leq
	\abs{Y^N_s - Y_s}.
\end{equation}
\end{itemize}

So that the following bound holds

\begin{equation}
	\abs{u^N(s, \psi^N(s, Y^N_s)) - u(s, \psi(s, Y_s))}
	\leq
  2\kappa_\rho \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}},
  +
	\abs{Y^N_s - Y^N}.
	\label{eq:bound_u_abs}
\end{equation}

Now for the third term in
\ref{eq:local_time_diff}
by adding and subtracting terms and using the triangle inequality

\begin{equation}
  \begin{split}
				\abs{\nabla u^N(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s))}
				&\leq
				\abs{\nabla u^N(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi^N(s, Y^N_s))}
				\\
				&+
				\abs{\nabla u(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi(s, Y^N_s))}
				\\
				&+
				\abs{\nabla u(s, \psi(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s))}
  \end{split}.
\end{equation}

The terms on the right hand side will be bounded as follows:

\begin{itemize}
\item
For the first term we use
\ref{prop:diff_uN_graduN}
and we have

\begin{equation}
  \begin{split}
    \abs{\nabla u^N(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi^N(s, Y^N_s))}
		&\leq
    \norm{\nabla u^N(s) - \nabla u(s) }_{L^\infty}
		\\
		&\leq
	  \kappa_\rho \norm{ b - b^N }_{C_T\mathcal{C}^{-\beta}},
  \end{split}
\end{equation}

for the second term see that
$\nabla u, \nabla u^N$
are
$\alpha$-Hölder
continuous and using
\ref{prop:bound_psi-psiN}
we have

\begin{equation}
\begin{split}
  \abs{\nabla u(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi(s, Y^N_s))}
	&\leq
	\abs{\psi^N(s, Y^N_s) - \psi(s, Y^N_s)}^\alpha \norm{u}_{C_T \mathcal{C}^{1 + \alpha}}
	\\
	&\leq
	(2 \kappa_\rho \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}})^\alpha \norm{u}_{C_T \mathcal{C}^{1 + \alpha}}.
\end{split}
\end{equation}

\end{itemize}

Therefore we get the bound

\begin{equation}
  \begin{split}
				\abs{\nabla u^N(s, \psi^N(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s))}
				&\leq
	  \kappa_\rho \norm{ b - b^N }_{C_T\mathcal{C}^{-\beta}}
				\\
				&+
	2^\alpha \kappa_\rho^\alpha \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}}^\alpha \norm{u}_{C_T \mathcal{C}^{1 + \alpha}}
				\\
				&+
				\abs{\nabla u(s, \psi(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s))}
  \end{split}.
	\label{eq:bound_gradu_abs}
\end{equation}

Using the bounds for
\ref{eq:bound_u_abs}
and
\ref{eq:bound_gradu_abs}
and the inequality

\begin{equation}
				(x_1 + \dotsm + x_k)^2 \leq k(x_1 + \dotsm + x_k),
\end{equation}

for some
$k$,
we get

\begin{equation}
\begin{split}
&\mathbb{E} [L^0_t ( Y^N - Y)]
\\
&\leq
4 \epsilon
+
4 (1 + \lambda) \qty(
  2\kappa_\rho \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}} t
  +
	\mathbb{E}\qty[\int_0^t\abs{Y^N_s - Y^N} ds]
	)
	\\
&+
\frac{1}{\epsilon} 3 t \norm{ b^N - b}_{C_T \mathcal{C}^{-\beta}}
\qty(
\kappa_\rho^2 \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}}
+
4(2 \kappa_\rho)^{2 \alpha} \norm{u}_{C_T \mathcal{C}^{1 + \alpha}}^2 \norm{b^N - b}_{C_T \mathcal{C}^{-\beta}}^{2 \alpha - 1}
)
\\
&+
\frac{1}{\epsilon} 3 \mathbb{E}
\qty(
			\int_0^t \mathbbm{1}_\qty{Y^N_s - Y_s > \epsilon} e^{1 - \frac{Y^N_s - Y_s}{\epsilon}} \qty| \nabla u(s, \psi(s, Y^N_s)) - \nabla u(s, \psi(s, Y_s))|^2 ds
)
\end{split}
\end{equation}

\ei{
				add explanation of why you use absolute value of difference of
				$ u^N - u $,
				because in
				\cref{eq:local_time_diff}
				there is no absolute value
}

\ei{
				explain hoy you get 4 in fron of
				$ (1 + \lambda) $
				by bounding the indicator functions under the integral in
				\cref{eq:local_time_diff}
}

\section{Convergence rate of the solution to the regularised SDE and the original}
